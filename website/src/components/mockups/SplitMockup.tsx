import { InteractiveMockup } from '../InteractiveMockup';

export const SplitMockup = () => {
  return (
    <InteractiveMockup 
      feature="split" 
      bg="hyper_space" 
      scale={0.78}
      className="h-full w-full" 
    />
  );
};
