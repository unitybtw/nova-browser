import { InteractiveMockup } from '../InteractiveMockup';

export const ShieldMockup = () => {
  return (
    <InteractiveMockup 
      feature="shield" 
      bg="aurora_waves" 
      scale={0.78}
      className="h-full w-full" 
    />
  );
};
