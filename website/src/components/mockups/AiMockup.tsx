import { InteractiveMockup } from '../InteractiveMockup';

export const AiMockup = () => {
  return (
    <InteractiveMockup 
      feature="ai" 
      bg="nebula" 
      scale={0.78}
      className="h-full w-full" 
    />
  );
};
